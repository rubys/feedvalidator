<h2>Congratulations!</h2>

<p><img alt="[Valid %(feedType)s]" title="Valid %(feedType)s" src="images/%(graphic)s" width="88" height="31" /> This is a valid %(feedType)s feed.</p>

<p>If you would like to create a banner that links to this page (i.e. this validation result), do the following:</p>

<ol>
<li><p>Download <a href="images/%(graphic)s">the "valid %(feedType)s" banner</a>.</p></li>
<li><p>Upload the image to your own server.  (This step is important.  Please do not link directly to the image on this server.)</p></li>
<li><p>Add this HTML to your page (change the image <code>src</code> attribute if necessary):</p>
<blockquote>
<p>
<textarea rows="5" cols="72">&lt;a href="%(HOMEURL)s?url=%(url)s"&gt;&lt;img src="%(graphic)s" alt="[Valid %(feedType)s]" title="Validate my %(feedType)s feed" width="88" height="31" /&gt;&lt;/a&gt;</textarea>
</p>
</blockquote></li>
</ol>

<p>If you would like to create a text link instead, here is the URL you can use:</p>

<blockquote>
<p><code><a href="%(HOMEURL)s?url=%(url)s">%(HOMEURL)s?url=%(url)s</a></code></p>
</blockquote>
